# USDM MindMap Editor - Issue List

本書は要求仕様書で扱う検討事項、決定内容、対応状態を管理する Issue 一覧である。

| 番号 | 項目名 | 優先度 | 状態 |
|---|---|---|---|
| 001 | 背景ノードの扱い | 高 | 決定済み |
| 002 | 要求の多重親子関係 | 高 | 決定済み |
| 003 | 仕様の共有 | 高 | 決定済み |
| 004 | ノード座標保存 | 高 | 決定済み |
| 005 | 自動レイアウト | 中 | 決定済み |
| 006 | 接続線デザイン | 低 | 決定済み |
| 007 | ノード配色 | 低 | 決定済み |
| 008 | 理由の表示方法 | 中 | 決定済み |
| 009 | ID上限 | 低 | 決定済み |
| 010 | Undo / Redo | 中 | 決定済み |
| 011 | 背景IDの開始値 | 中 | 決定済み |
| 012 | YAMLスキーマバージョン | 中 | 未決定 |
| 013 | ノード削除時の子要素の扱い | 高 | 決定済み |
| 014 | 理由を必須項目にするか | 中 | 決定済み |
| 015 | ファイル保存方式 | 高 | 決定済み |
| 016 | UI自動テストの導入タイミング | 低 | 未決定 |
| 017 | コピー＆ペースト | 低 | 決定済み |
| 018 | ノード検索 | 中 | 決定済み |
| 019 | YAMLフォーマット | 高 | 未決定 |
| 020 | USDM表への変換 | 中 | 決定済み |
| 021 | Git差分を考慮した保存 | 高 | 決定済み |
| 022 | 大規模データ対応 | 中 | 未決定 |
| 023 | 将来拡張 | 低 | 未決定 |

---

# Issue #001 背景ノードの扱い

## 現状

背景は255件まで登録可能としているが、
画面上でどのように配置・表示するかが未定義。

## 決定

案1を採用する。背景ノードを最上位としてキャンバスに表示し、
各上位要求は必ず1つの背景ノードに属するものとする。

上位要求に複数の背景ノードを親として設定することは認めない。

## 候補

### 案1

背景ノードを最上位として表示する。

```
背景
 ↓
上位要求
```

### 案2

背景ノードを独立して複数表示する。

```
背景1
背景2
背景3

 ↓

上位要求
```

### 案3

背景は画面左上に一覧表示し、
グラフには表示しない。

## 優先度

高

## 状態

決定済み

---

# Issue #002 要求の多重親子関係

## 現状

下位要求は親を1つだけ持つ前提となっている。

しかし実際には

```
要求A
   \
    \
     下位要求
    /
要求B
```

のように
複数要求から参照されるケースがある。

## 決定

下位要求は必ず1つの上位要求に属するものとする。
複数の上位要求を親として設定することは認めない。

## 検討事項

- 親は1つに限定するか
- 複数親を許可するか

## 優先度

高

## 状態

決定済み

---

# Issue #003 仕様の共有

## 現状

仕様は1つの下位要求に属する前提。

しかし

```
要求A
   \
    \
    仕様
    /
要求B
```

というケースが存在する。

## 決定

仕様は必ず1つの下位要求に属するものとする。
複数の下位要求を親として設定することは認めない。

所属先以外の下位要求との関係は、所有関係ではなく任意の関連情報として保存できる。
YAML では `related_requirement_ids` に関連する下位要求 ID を配列で保存する。
この関連情報は仕様の親子関係や ID 採番を変更しない。

```yaml
specifications:
  - id: "001-001-001"
    specification: 編集内容を一定時間ごとに保存する
    parent_requirement_id: "001-001"
    related_requirement_ids:
      - "002-001"
```

## 検討事項

仕様を共有可能にするか。

## 優先度

高

## 状態

決定済み

---

# Issue #004 ノード座標保存

## 現状

YAMLへノード座標を保存するか未定。

## 決定

初期版ではノード座標を YAML に保存しない。
ノードは新規追加時と YAML 読み込み時に自動配置する。
手動でノード位置を変更する機能は初期版では提供しない。

## 候補

保存する。

```yaml
position:
  x: 120
  y: 350
```

保存しない。

毎回自動配置。

## 優先度

高

## 状態

決定済み

---

# Issue #005 自動レイアウト

## 現状

ノード追加後の配置方法が未定。

## 決定

初期版は自動整列かつ自動配置とする。
ノードの追加、削除、YAML 読み込み後にレイアウトを再計算し、手動配置は提供しない。

## 候補

- 自動整列
- 手動移動
- 両方対応

## 推奨

両対応

## 優先度

中

## 状態

決定済み

---

# Issue #006 接続線デザイン

## 現状

線の描画方法が未定。

## 決定

接続線はベジェ曲線とする。

## 候補

- 直線
- 折れ線
- ベジェ曲線

## 推奨

ベジェ曲線

## 優先度

低

## 状態

決定済み

---

# Issue #007 ノード配色

## 現状

ノード色未定。

## 決定

初期版の配色は以下とする。

- 背景: 青
- 要求: 緑
- 仕様: 黄
- ノードの文字と枠線: 黒
- キャンバス: 白

ユーザーによる配色設定は将来対応とする。

## 候補

背景：青

要求：緑

仕様：黄

または

すべて白

## 優先度

低

## 状態

決定済み

---

# Issue #008 理由の表示方法

## 現状

理由を常時表示するか未定。

## 決定

理由はデフォルトで折りたたんで表示する。
上位要求または下位要求ノードの右端に V ボタンを配置し、押下時に理由を展開または折りたたむ。

## 候補

常時表示

折り畳み可能

マウスホバー表示

## 推奨

折り畳み可能

## 優先度

中

## 状態

決定済み

---

# Issue #009 ID上限

## 現状

255件までと定義している。

## 決定

初期版の上限は各階層あたり255件とする。
上限不足が判明した場合は、将来版で65535件などへ拡張する。

## 検討事項

255固定

または

999件まで対応

## 優先度

低

## 状態

決定済み

---

# Issue #010 Undo / Redo

## 現状

操作取り消し機能が未定。

## 決定

初期版は macOS を対象とし、以下のショートカットを使用する。

- 操作取り消し: `Cmd+Z`
- 取り消しを戻す: `Cmd+Y`

## 推奨

Ctrl+Z

Ctrl+Shift+Z

macOS 前提では以下も候補とする。

- Cmd+Z
- Cmd+Shift+Z

## 優先度

中

## 状態

決定済み

---

# Issue #011 背景IDの開始値

## 現状

要求仕様書では背景 ID が `000～255` と記載されている。

一方、YAML 例では背景 ID に `001` が使用されている。

## 決定

背景 ID は `001` から開始し、上位要求、下位要求、仕様と同じ表記規則に統一する。
`000` はアプリケーションでは使用しない。

## 検討事項

- 背景 ID を `000` から開始するか
- 背景 ID を `001` から開始するか
- 背景と上位要求で同じ ID 体系を使ってよいか

## 推奨

背景 ID も `001` 開始に統一する。

理由は、上位要求、下位要求、仕様と表記規則を揃えられるため。

## 優先度

中

## 状態

決定済み

---

# Issue #012 YAMLスキーマバージョン

## 現状

YAML にスキーマバージョンを保存するか未定。

## 検討事項

- `version` を必須にするか
- 既存 YAML との互換性をどこまで考慮するか
- 将来、複数親や共有仕様に対応する場合の移行方法

## 推奨

`version: "1"` を保存する。

## 優先度

中

---

# Issue #013 ノード削除時の子要素の扱い

## 現状

ノード削除時に、配下の下位要求や仕様をどう扱うか未定。

## 決定

親ノードを削除する場合は、配下の子要素をすべて削除する。
削除前には確認ダイアログを表示し、削除対象の件数を示す。

確認ダイアログには「今後この確認を表示しない」チェックボックスを設ける。
チェックした場合、同じブラウザでは次回以降の削除確認を表示しない。

## 検討事項

- 親ノード削除時に子ノードも削除するか
- 子ノードを別の親へ移動できるようにするか
- 削除前に確認ダイアログを表示するか

## 推奨

初期実装では、親ノード削除時に配下要素も削除する。

削除前に確認ダイアログを表示し、削除対象件数を表示する。

## 優先度

高

## 状態

決定済み

---

# Issue #014 理由を必須項目にするか

## 現状

要求仕様書では要求ノードの項目として「理由」が定義されているが、入力必須かどうかは未定。

## 決定

理由は必須項目としない。
YAML 保存時は理由が空でも警告を表示せずに保存する。

USDM 表、Excel などのファイルエクスポート時は、理由が空の要求を警告として表示する。
この警告はエクスポートを妨げず、ユーザーは内容を確認したうえで出力を続行できる。

## 検討事項

- 理由を必須にするか
- 空欄を許可するか
- 空欄の場合に警告だけ出すか

## 推奨

USDM の性質上、理由は必須とする。

ただし、編集中の一時保存では空欄を許可し、保存前検証で警告またはエラーにする案も検討する。

## 優先度

中

## 状態

決定済み

---

# Issue #015 ファイル保存方式

## 現状

Web アプリケーションとして YAML を保存する場合の方式が未定。

## 決定

初期版はブラウザのダウンロードとして YAML ファイルを保存する。
Google Drive からの作成・保存は将来対応とし、初期版では扱わない。

## 検討事項

- ブラウザのダウンロードとして保存するか
- File System Access API を利用するか
- サーバー側の特定ディレクトリへ保存するか

## 推奨

初期実装ではブラウザのダウンロードとして保存する。

将来的に、対応ブラウザでは File System Access API による上書き保存を検討する。

## 優先度

高

## 状態

決定済み

---

# Issue #016 UI自動テストの導入タイミング

## 現状

UI 操作テストを導入するか未定。

## 検討事項

- Playwright を導入するか
- 初期段階は手動確認にするか
- YAML 入出力など Core 層テストを優先するか

## 推奨

初期段階では Core 層テストを優先する。

キャンバス操作が安定した段階で Playwright による主要操作テストを追加する。

## 優先度

低

---

# Issue #017 コピー＆ペースト

## 現状

ノード複製機能未定。

## 決定

macOS 版では、選択中ノードのコピーとペーストに `Cmd+C`、`Cmd+V` を使用する。
Windows 版のコピー＆ペーストは初期版では実装しない。

## 検討事項

Ctrl+C

Ctrl+V

対応

## 優先度

低

## 状態

決定済み

---

# Issue #018 ノード検索

## 現状

検索機能未定。

## 決定

検索対象は要求、理由、仕様の3項目とする。ID は検索対象に含めない。
また、要求・理由・仕様を横断して検索するフリーワード検索を提供する。

## 検討事項

- ID検索
- 要求検索
- 理由検索
- 仕様検索

## 優先度

中

## 状態

決定済み

---

# Issue #019 YAMLフォーマット

## 現状

内部構造は概略のみ。

## 検討事項

- バージョン番号
- UUID採用
- 将来互換性

## 優先度

高

---

# Issue #020 USDM表への変換

## 現状

将来対応予定。

## 決定

エクスポートメニューに USDM 表への変換を用意する。
変換結果はダウンロードできる形式で出力する。

## 検討事項

YAMLからUSDM表へ変換可能な構造とする。

## 優先度

中

## 状態

決定済み

---

# Issue #021 Git差分を考慮した保存

## 現状

Git管理を前提としている。

## 決定

変更差分を最小化するため、YAML は種別ごとのフラットな一覧構造で保存する。
シリアライザーは、トップレベルキー、各ノードの項目、配列を固定順で出力する。

- トップレベルキー: `version`、`title`、`backgrounds`、`requirements`、`specifications`
- ノード一覧: ID 昇順
- ノード項目: ID、本文、親 ID、関連 ID の順
- `related_requirement_ids`: ID 昇順
- 任意項目が空の場合: 出力しない
- YAML のインデント: 半角スペース2個、改行コード: LF
- 保存日時や自動生成 ID など、内容と無関係な情報は保存しない

## 検討事項

変更差分が最小になるYAML構造を設計する。

## 優先度

高

## 状態

決定済み

---

# Issue #022 大規模データ対応

## 現状

数百～数千ノードでの動作未定。

## 検討事項

- 描画性能
- スクロール性能
- メモリ消費

## 優先度

中

---

# Issue #023 将来拡張

## 想定機能

- Excel出力
- Markdown出力
- SVG出力
- PNG出力
- PDF出力
- 要求トレーサビリティ表示
- タグ管理
- レビュー状態管理
- 優先度管理
- コメント機能
- Git連携

## 優先度

低
